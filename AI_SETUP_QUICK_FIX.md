# ⚡ Быстрое исправление: "AI service is not configured"

## Проблема
После добавления `OPENAI_API_KEY` в `.env.local` видите ошибку:
```
AI service is not configured. Please contact support.
```

## ✅ Решение

### Next.js не подхватил новые env переменные!

**Нужно перезапустить dev сервер:**

```bash
# 1. Остановите текущий dev сервер
# Нажмите Ctrl+C в терминале где запущен npm run dev

# 2. Перезапустите dev сервер
npm run dev
```

---

## 🔍 Почему это происходит?

Next.js загружает environment variables **только при старте** процесса.

Если вы:
1. ✅ Добавили `OPENAI_API_KEY` в `.env.local`
2. ❌ Но НЕ перезапустили `npm run dev`

То Next.js **НЕ ВИДИТ** новую переменную!

---

## ✅ Проверка что всё работает

### 1. Перезапустите dev сервер
```bash
npm run dev
```

### 2. Проверьте логи в терминале
Должны увидеть:
```
Supabase client configuration { url: true, anonKey: true, serviceRoleKey: false }
Supabase client created successfully
```

### 3. Откройте форму создания события
```
http://localhost:3000/events/create
```

### 4. Нажмите кнопку "Сгенерировать правила (ИИ)"

Должен появиться:
- ✅ Диалог подтверждения (новое!)
- ✅ После подтверждения - генерация правил (2-5 секунд)
- ✅ Правила появляются в textarea

---

## 📝 Ваш `.env.local` должен содержать:

```env
# Supabase (обязательно)
NEXT_PUBLIC_SUPABASE_URL=https://djbqwsipllhdydshuokg.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...

# OpenAI для AI-функций (обязательно для генерации правил)
OPENAI_API_KEY=sk-proj-...
OPENAI_MODEL=gpt-4o-mini

# Dev
DEV_USER_ID=00000000-0000-0000-0000-000000000001
```

---

## 🚨 Если всё равно не работает

### Проблема 1: Неправильный OPENAI_API_KEY
**Симптом:** Ошибка "AI service authentication failed"

**Решение:**
1. Проверьте что ключ начинается с `sk-proj-`
2. Получите новый ключ: https://platform.openai.com/api-keys
3. Замените в `.env.local`
4. Перезапустите dev сервер

### Проблема 2: Закончились деньги на OpenAI
**Симптом:** Ошибка "AI service is currently busy"

**Решение:**
1. Откройте: https://platform.openai.com/settings/organization/billing
2. Пополните баланс (минимум $5)
3. Подождите 1-2 минуты

### Проблема 3: Dev сервер не перезапустился
**Симптом:** Всё равно "AI service is not configured"

**Решение:**
```bash
# Убейте все Node процессы
pkill -f "next dev"

# Перезапустите
npm run dev
```

### Проблема 4: Опечатка в имени переменной
**Проверьте:** Переменная должна называться **ТОЧНО**:
```env
OPENAI_API_KEY=...
```

❌ НЕ `OPEN_AI_API_KEY`  
❌ НЕ `OPENAI_KEY`  
❌ НЕ `NEXT_PUBLIC_OPENAI_API_KEY`

---

## ✨ Что нового в этом обновлении

### 1. Диалог подтверждения перед генерацией
Теперь при нажатии на кнопку "Сгенерировать правила (ИИ)" показывается диалог:

```
📋 Генерация правил с помощью ИИ

Перед генерацией рекомендуется заполнить всю форму, так как ИИ учитывает следующие данные:

• Название и описание события
• Категорию и тип автомобиля
• Допустимые марки авто
• Дополнительные поля регистрации
• Платность события

Чем больше информации вы укажете, тем более точными и релевантными будут сгенерированные правила.

[Отмена]  [Продолжить генерацию]
```

**Зачем это?**
- Напоминает пользователю заполнить форму полностью
- Объясняет какие данные использует AI
- Предотвращает случайные нажатия
- Улучшает UX и качество генерации

### 2. Улучшенная валидация
Теперь перед показом диалога проверяются обязательные поля:
- ✅ Название события
- ✅ Категория
- ✅ Город

Если что-то не заполнено - показывается ошибка валидации вместо диалога.

---

## 🧪 Тестирование

1. ✅ Перезапустите dev сервер
2. ✅ Откройте `/events/create`
3. ✅ Заполните обязательные поля
4. ✅ Нажмите "Сгенерировать правила (ИИ)"
5. ✅ Проверьте что появился диалог
6. ✅ Нажмите "Продолжить генерацию"
7. ✅ Проверьте что правила загрузились

---

## 📊 Стоимость

**Модель:** gpt-4o-mini  
**Стоимость:** ~$0.0004 за генерацию  
**Очень дешево!** 🎉

---

## 🆘 Нужна помощь?

Если проблема не решилась:
1. Проверьте логи в терминале dev сервера
2. Проверьте консоль браузера (F12)
3. Убедитесь что `.env.local` в корне проекта
4. Убедитесь что перезапустили dev сервер

Всё ещё не работает? Напишите в поддержку! 💬
