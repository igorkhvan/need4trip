# 🔍 ДИАГНОСТИКА: Таблица club_plans отсутствует в типах

## Проблема
```
Argument of type '"club_plans"' is not assignable to parameter type
```

## Что я нашёл

### ✅ Миграция существует
`supabase/migrations/20241213_create_club_plans.sql`
- Создаёт таблицу `club_plans`
- 3 плана: free, basic, pro
- Все колонки соответствуют `DbClubPlan` интерфейсу

### ❌ Таблица НЕ в Supabase типах
`src/lib/types/supabase.ts` содержит:
- events ✅
- clubs ✅
- cities ✅
- currencies ✅
- club_plans ❌ (ОТСУТСТВУЕТ!)

### ✅ Локальный интерфейс есть
`src/lib/types/clubPlan.ts` - интерфейс `DbClubPlan` полностью соответствует структуре БД

## 🎯 Правильное решение (БЕЗ костылей!)

### Вариант 1: Применить миграцию в Supabase (правильно)
```bash
# В Supabase Dashboard → SQL Editor
# Выполнить содержимое: supabase/migrations/20241213_create_club_plans.sql

# Затем регенерировать типы:
supabase gen types typescript --project-id YOUR_REF > src/lib/types/supabase.ts
```

### Вариант 2: Проверить статус миграций
Возможно миграция УЖЕ применена, но типы не обновлены?

```bash
# Проверить в Supabase Dashboard → Database → Tables
# Есть ли таблица club_plans?
```

## ⚠️ Что я НЕ буду делать

❌ `(supabase as any).from('club_plans')` - КОСТЫЛЬ
❌ Сложные type extensions с `as unknown as` - КОСТЫЛЬ
❌ Игнорировать проблему - НЕ РЕШЕНИЕ

## 📝 Что нужно от тебя

1. Проверь в Supabase Dashboard → Database → Tables
   - Есть ли таблица `club_plans`?
   
2. Если ДА → просто регенерируй типы (как мы делали раньше)
3. Если НЕТ → примени миграцию, потом регенерируй типы

После регенерации типов я уберу временный fix и всё будет работать!
