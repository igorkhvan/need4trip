---
alwaysApply: true
---

# Need4Trip - SSOT Rules (Single Source of Truth)

**Last Updated:** 27 декабря 2024  
**Status:** Production Ready ✅

---

## 🎯 ГЛАВНЫЙ ПРИНЦИП

> **ПЯТЬ SSOT ДОКУМЕНТОВ — ЕДИНСТВЕННЫЕ источники истины:**
> 1. `docs/ARCHITECTURE.md` — архитектура, паттерны, ownership
> 2. `docs/DATABASE.md` — структура БД, таблицы, индексы, RLS
> 3. `docs/BILLING_SYSTEM_ANALYSIS.md` — биллинг, тарифы, лимиты
> 4. `docs/TESTING.md` — тестирование, тест-кейсы, результаты
> 5. `docs/DESIGN_SYSTEM.md` — UI/UX, модалки, цвета, компоненты ✨

---

## 🔄 СИНХРОНИЗАЦИЯ MEMORY ↔ RULES FILES

**КРИТИЧНО:** Memory правила (в базе Cursor) и Rules файлы (`.cursor/rules/*.mdc`) должны быть ВСЕГДА синхронизированы.

### ПОСЛЕ создания/обновления Memory правила:
1. ✅ Проверь соответствующий rules файл
2. ✅ Обнови файл с новой информацией из Memory
3. ✅ Коммить обновленный rules файл
4. ✅ В commit message: "sync Memory → Rules"

### ПОСЛЕ обновления rules файла:
1. ✅ Проверь соответствующие Memory правила
2. ✅ Обнови Memory через `update_memory` если нужно
3. ✅ Убедись что нет разногласий

**Mapping:**
- DATABASE.md SSOT (Memory) → `need4trip-ssot-rules.mdc` (секция DATABASE.md)
- BILLING SSOT (Memory) → `need4trip-ssot-rules.mdc` (секция BILLING)
- ARCHITECTURE SSOT (Memory) → `need4trip-ssot-rules.mdc` (секция ARCHITECTURE)
- TESTING SSOT (Memory) → `need4trip-ssot-rules.mdc` (секция TESTING)
- DESIGN_SYSTEM SSOT (Memory) → `need4trip-ssot-rules.mdc` (секция DESIGN) ✨

**Причина:** Rules файлы видны пользователю, Memory — только AI. Синхронизация = консистентность.

---

## 📋 ОБЯЗАТЕЛЬНЫЙ WORKFLOW

### ПЕРЕД любым изменением:

1. **Определи область:**
   - Архитектура/паттерны → читай `ARCHITECTURE.md`
   - База данных → читай `DATABASE.md`
   - Биллинг/лимиты → читай `BILLING_SYSTEM_ANALYSIS.md`
   - Тестирование → читай `TESTING.md`
   - UI/UX/дизайн → читай `DESIGN_SYSTEM.md` ✨

2. **Изучи SSOT:**
   - Найди соответствующий раздел
   - Пойми текущие правила и ограничения
   - Проверь ownership map

3. **Спланируй изменения:**
   - Соответствуют ли они принципам SSOT?
   - Не нарушают ли существующие контракты?
   - Обратная совместимость гарантирована?

### ПОСЛЕ изменения:

1. **Обнови SSOT:**
   - Если изменилась архитектура → обнови `ARCHITECTURE.md`
   - Если изменилась БД → обнови `DATABASE.md`
   - Если изменился биллинг → обнови `BILLING_SYSTEM_ANALYSIS.md`
   - Если изменились тесты → обнови `TESTING.md`
   - Если изменился UI/дизайн → обнови `DESIGN_SYSTEM.md` ✨

2. **Коммит:**
   - Код + обновленный SSOT в ОДНОМ коммите
   - Коммит message указывает что обновлен SSOT

3. **Verification:**
   - `npx tsc --noEmit` ✅
   - `npm run build` ✅
   - SSOT отражает актуальное состояние ✅

---

## 🏗️ ARCHITECTURE.md (SSOT)

**Что содержит:**
- Module ownership map (кто отвечает за что)
- Data access patterns (Repos → Services → API)
- Runtime boundaries (Server vs Client, Edge vs Node)
- Caching strategy (StaticCache для reference data)
- Auth & currentUser rules
- Type safety contracts

**Ключевые паттерны:**

```typescript
// ✅ DB Access (SSOT: src/lib/db/client.ts)
const db = getAdminDb(); // гарантированно non-null

// ✅ Date/Time (SSOT: src/lib/utils/dates.ts)
import { formatDateTime, parseDateTime } from '@/lib/utils/dates';

// ✅ Hydration (SSOT: src/lib/utils/hydration.ts)
const events = await hydrateCities(rawEvents);

// ✅ Visibility (SSOT: src/lib/utils/eventVisibility.ts)
const canView = await canViewEvent(event, currentUser);

// ✅ Permissions (SSOT: src/lib/utils/eventPermissions.ts)
const canRegister = await canRegisterForEvent(event, currentUser);

// ✅ Caching (SSOT: src/lib/cache/staticCache.ts)
const cache = new StaticCache<T>({ ttl, name }, loader, keyExtractor);
```

**ЗАПРЕЩЕНО:**
- ❌ Дублировать логику (всегда используй canonical модуль)
- ❌ UI → Repository напрямую (только через Services)
- ❌ Server-only код в client components
- ❌ Множественные способы делать одно и то же

---

## 🗄️ DATABASE.md (SSOT)

**Что содержит:**
- 20 таблиц с полными CREATE TABLE схемами
- Performance indexes (compound, covering, partial)
- RLS policies summary для всех защищенных таблиц
- Database functions & triggers
- Migration history (60+ миграций)
- ERD диаграмма связей
- Maintenance rules

**SQL Миграции (СТРОГО!):**

```bash
# Формат имени (ОБЯЗАТЕЛЬНО с timestamp):
supabase/migrations/YYYYMMDD_description.sql

# Пример:
supabase/migrations/20241225_add_event_tags.sql
```

**Workflow:**
1. Читай `DATABASE.md` перед изменением
2. Создай миграцию с timestamp
3. Примени миграцию в Supabase
4. Обнови `DATABASE.md` с новой структурой
5. Коммить миграцию + `DATABASE.md` вместе

**ЗАПРЕЩЕНО:**
- ❌ SQL файлы без timestamp в названии
- ❌ SQL файлы в корне проекта
- ❌ SQL файлы с UPPERCASE названиями (APPLY_VIA_DASHBOARD*.sql)
- ❌ Временные файлы типа `.temp_migrations/`
- ❌ Изменение уже примененных миграций
- ❌ Прямые изменения в БД без миграции

---

## 💳 BILLING_SYSTEM_ANALYSIS.md (SSOT)

**Что содержит:**
- Тарифы (free, club_50, club_500, club_unlimited)
- Лимиты для каждого тарифа
- Enforcement логика (enforceClubAction)
- PaywallError структура
- Frontend интеграция (PaywallModal)
- Grace period правила

**Ключевые принципы:**

1. **Frontend НЕ решает лимиты:**
   - Фронт только показывает UI
   - Реагирует на 402 от backend
   - Использует `PaywallModal.tsx` (типизированный)

2. **Backend — единственный источник истины:**
   - `enforceClubAction()` в `src/lib/services/accessControl.ts`
   - Проверяет текущую подписку клуба
   - Throws `PaywallError` (402) если лимит превышен

3. **Billing transactions — только аудит:**
   - НЕ участвуют в проверках доступа
   - Используются для истории платежей

**Пример использования:**

```typescript
// Backend (API Route)
import { enforceClubAction } from '@/lib/services/accessControl';

await enforceClubAction({
  clubId: event.clubId,
  action: 'CREATE_EVENT_WITH_PARTICIPANTS',
  context: { eventParticipantsCount: 100 }
}); // throws PaywallError если превышен лимит

// Frontend (Component)
import { usePaywall } from '@/components/billing/PaywallModal';

const { showPaywall, PaywallModalComponent } = usePaywall();

try {
  await createEvent(...);
} catch (err) {
  if (err.response?.status === 402) {
    showPaywall(err.response.data.error.details);
  }
}

return <>{PaywallModalComponent}</>;
```

**ЗАПРЕЩЕНО:**
- ❌ Проверки лимитов на фронтенде
- ❌ Использовать старый `paywall-modal.tsx` (удален)
- ❌ Изменять логику enforcement без обновления SSOT

---

## 🧪 TESTING.md (SSOT)

**Что содержит:**
- 53 automated tests (43 integration + 2 skipped + 8 E2E TODO)
- QA-1 to QA-8: Core billing v4 tests (publish enforcement, SSOT verification)
- QA-9 to QA-15: Event create/update enforcement (Production Bug Fix 2024-12-26) ⚡
- QA-16 to QA-45: Extended coverage (API routes, webhooks, boundaries)
- QA-46 to QA-53: E2E Playwright tests (TODO)
- Test configuration (Jest, .env.test, helpers)
- Execution results (~35 seconds для всех integration tests)
- Continuous testing guidelines

**Production Bug Fix (2024-12-26):** ⚡
Plan limit enforcement в `createEvent()`/`updateEvent()` дублировала проверку из publish endpoint, блокируя one-off credit flow для редактирования событий 16-500 участников.

**Solution:**
- create/update проверяют только `maxParticipants > 500` (impossible state)
- `POST /api/events/:id/publish` — единственный источник истины для монетизации
- Personal events 16-500 сохраняются успешно, monetization check в publish

**Команды:**
```bash
npm test                    # Все integration tests
npm test -- billing.v4      # Core billing tests  
npm test -- events.enforcement  # Production bug fix tests
npm run test:e2e           # E2E Playwright tests (TODO)
```

**Status (2024-12-26):**
✅ 15/15 integration tests PASS (QA-1 to QA-15)
⏱️ Total: ~35 seconds
🗄️ Production Supabase
✅ Production bug fixed and tested

**Правило:** Перед изменением тестов читай `docs/TESTING.md`. После изменений обновляй SSOT + коммить вместе.

---

## 📁 Session Documents (Временные)

**Назначение:** Аналитические документы для больших задач

**Структура:**
```
docs/sessions/
├── YYYY-MM-DD-название/
│   ├── analysis.md       # Начальный анализ
│   ├── implementation.md # Детали реализации
│   ├── stages-N-M.md    # Промежуточные отчёты
│   └── complete.md      # Финальный отчёт
└── README.md            # Правила работы
```

**Workflow:**
1. **СОЗДАНИЕ:** AI создаёт папку для контекста большой задачи
2. **ЗАВЕРШЕНИЕ:** Ключевые insights переносятся в SSOT
3. **CLEANUP:** Sessions старше 30 дней можно удалять

**Правила:**
- ✅ Создавай session для больших задач (НЕ для мелких изменений)
- ✅ Переноси insights в SSOT после завершения
- ✅ Удаляй старые sessions периодически
- ❌ НЕ используй sessions как источник истины
- ❌ НЕ ссылайся на них из кода/документов
- ❌ НЕ храни критичную информацию только в sessions

---

## 🎨 DESIGN_SYSTEM.md (SSOT) ✨

**Что содержит:**
- Модальные окна (Dialog/AlertDialog) — production-ready паттерн
- Цветовая система (CSS variables)
- Типографика (Inter font, heading classes)
- Buttons (variants, sizes, states)
- Cards, Badges, другие компоненты
- Анимации (fade/zoom, БЕЗ translateY конфликтов)
- Адаптивность (mobile first, breakpoints)

**Ключевые паттерны:**

### Модалки (КРИТИЧНО!)

```tsx
// ✅ Правильная структура
<Dialog>
  <DialogContent className="sm:max-w-lg">
    <DialogHeader>
      <DialogTitle>Title</DialogTitle>
      <DialogDescription>Description</DialogDescription>
    </DialogHeader>

    <DialogBody className="space-y-4">
      {/* Scrollable content */}
    </DialogBody>

    <DialogFooter>
      <Button variant="outline">Cancel</Button>
      <Button>Confirm</Button>
    </DialogFooter>
  </DialogContent>
</Dialog>
```

**Анатомия:**
- DialogContent: `flex flex-col max-h-[90vh]`
- DialogHeader: `px-4 pt-6 pb-4 border-b` (fixed)
- DialogBody: `flex-1 overflow-y-auto px-4 py-4` (scrollable)
- DialogFooter: `px-4 py-4 border-t` (fixed)

**Анимации:**
```tsx
// ✅ ПРАВИЛЬНО: fade + zoom
"data-[state=open]:fade-in-0 data-[state=open]:zoom-in-95"

// ❌ НЕПРАВИЛЬНО: slide-in конфликтует с центрированием
"slide-in-from-top-[48%]"  // НЕ ИСПОЛЬЗОВАТЬ!
```

### Цвета

```tsx
// ✅ Используй CSS variables
<Button className="bg-[var(--color-primary)]">

// ❌ НЕ hardcode hex
<Button className="bg-[#FF6F2C]">
```

**Палитра:**
- `--color-primary` (#FF6F2C) — основной оранжевый
- `--color-success` (#22C55E) — зелёный
- `--color-warning` (#F59E0B) — жёлтый
- `--color-danger` (#EF4444) — красный

**ЗАПРЕЩЕНО:**
- ❌ НЕ используй grid на DialogContent (только flex flex-col)
- ❌ НЕ ставь padding на DialogContent (только на header/body/footer)
- ❌ НЕ используй slide-in-from-top анимации
- ❌ НЕ ставь overflow-y-auto на DialogContent (только на DialogBody)
- ❌ НЕ hardcode цвета (используй CSS variables)

**ОБЯЗАТЕЛЬНО:**
- ✅ Всегда используй DialogBody для контента
- ✅ max-h только на DialogContent
- ✅ Кнопки w-full sm:w-auto для адаптивности
- ✅ Primary кнопка справа/снизу в footer

**Правило:** ВСЕГДА читай `DESIGN_SYSTEM.md` перед UI изменениями. ВСЕГДА обновляй его после успешных изменений.

---

## 🎨 Дизайн-система (/figma/) — DEPRECATED

**Статус:** Исключена из билда, в .gitignore

**НОВЫЙ SSOT:** `docs/DESIGN_SYSTEM.md` ✨

**Документация (старая):** `/figma/src/DESIGN_SYSTEM.md`

**Цвета:**
- Primary: #FF6F2C (оранжевый)
- Success: #22C55E, Warning: #F59E0B, Danger: #EF4444

**Типографика:**
- Шрифт: Inter (Google Fonts)
- h1: 32px (desktop) / 36px (mobile)
- Body: 15px

**Компоненты:**
- Button (4 варианта), Badge (6 типов), Card (3 размера)
- Modal (5 размеров), Input/Select (высота 48px)

**Правило:** Старая figma папка для reference. **Используй `docs/DESIGN_SYSTEM.md` как SSOT.**

---

## ✅ Vercel Compatibility (КРИТИЧНО!)

**Runtime boundaries:**

```typescript
// ✅ Server-only utilities
import { getAdminDb } from '@/lib/db/client'; // OK в API routes

// ❌ НИКОГДА в client components
'use client';
import { supabaseAdmin } from '@/lib/db/client'; // ❌ ОШИБКА БИЛДА
```

**Правила:**
- Server-only код НЕ должен импортироваться в `'use client'` компонентах
- Утилиты для обеих сторон должны быть pure functions
- No Node-only APIs в Edge runtimes
- Изоморфный код только для shared utilities

**Checklist перед push:**
```bash
npx tsc --noEmit  # TypeScript ✅
npm run build     # Production build ✅
git status        # Clean или tracked changes ✅
git push origin main
```

**Правило:** Если build не проходит — НЕ пушь. Сначала исправь.

---

## 🔧 Git Workflow

**После КАЖДОГО логического изменения:**

```bash
git add -A
git commit -m "type: description

Details...

Verified: TypeScript ✅, Build ✅"
```

**Conventional Commits format:**
- `feat:` — новая функциональность
- `fix:` — исправление бага
- `refactor:` — рефакторинг без изменения поведения
- `docs:` — только документация
- `chore:` — инфраструктура, зависимости

**Staged commits при рефакторинге:**
- НЕ "big rewrite" в одном коммите
- Один коммит = одна атомарная стадия с верификацией
- Пример: `refactor: consolidate dates (STAGE 1)`

**NO Breaking Changes без явного запроса:**
- Сохраняй сигнатуры через overloads, union типы
- Обратная совместимость обязательна

---

## 🚨 TypeScript Strict Mode

**Обязательный workflow перед использованием типа:**

```bash
# 1. Проверь что тип экспортируется
grep "^export.*TypeName" path/to/file.ts

# 2. Проверь возвращаемый тип функции
grep "functionName.*Promise" path/to/file.ts

# 3. Не предполагай типы по названию функции
```

**Правила:**
- НИКОГДА не используй `any`
- TypeScript strict mode ловит ошибки ДО рантайма
- Не ослабляй типы для "быстрого фикса"
- Если тип не экспортируется — он не существует для внешнего мира

---

## 📦 Ownership Map (из ARCHITECTURE.md)

| Topic | Canonical Module | Notes |
|-------|-----------------|-------|
| Date/Time | `lib/utils/dates.ts` | Все форматирование дат |
| Hydration | `lib/utils/hydration.ts` | Batch loading reference data |
| DB Access | `lib/db/client.ts` | `getAdminDb()` wrapper |
| Visibility | `lib/utils/eventVisibility.ts` | Event visibility checks |
| Permissions | `lib/utils/eventPermissions.ts` | Registration eligibility |
| Caching | `lib/cache/staticCache.ts` | Reference data caching |
| UI Formatting | `lib/utils/eventFormatters.ts` | Event price, etc |
| Billing Enforcement | `lib/services/accessControl.ts` | `enforceClubAction()` |

**Правило:** Перед добавлением кода открой Ownership Map, найди владельца concern, используй ТОЛЬКО канонический модуль.

---

## 🔄 Staged Refactoring Pattern

**Пример правильного подхода:**

```
STAGE 0: Inventory & Doc Foundation
STAGE 1: Date/Time Consolidation
STAGE 2: Supabase Admin DB Client
STAGE 3: Hydration Consolidation
STAGE 4: Visibility & Permissions
STAGE 5: UI Formatters
STAGE 6: Caching Strategy
```

**Каждая стадия:**
1. Изменения в коде
2. Обновление SSOT документов
3. TypeScript check
4. Production build
5. Git commit
6. Push to Vercel

**Правило:** Маленькие атомарные коммиты > один большой rewrite.

---

## ⚠️ STOP CONDITIONS

**Когда остановиться и спросить:**

1. Изменение может затронуть product behavior
2. Требуется breaking change в API
3. Vercel build fails
4. SSOT документы противоречат коду
5. Требуется доступ к env/secrets/CLI

**Правило:** Лучше спросить, чем сделать неправильно.

---

## 🎯 Definition of Done

Задача считается завершенной когда:

- [ ] Код реализован согласно SSOT
- [ ] SSOT документы обновлены (если нужно)
- [ ] TypeScript strict mode ✅
- [ ] Production build успешен ✅
- [ ] Нет linter errors
- [ ] Git commit с описанием изменений
- [ ] Push в Vercel (если требуется)

---

**Помни:** Архитектура существует чтобы СНИЗИТЬ когнитивную нагрузку, а не увеличить её.

**Single Source of Truth = Меньше мест для принятия решений = Меньше ошибок.**
