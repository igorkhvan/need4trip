# 🔍 АНАЛИЗ: 404 на "Создать событие"

**Дата**: 2025-12-22  
**Проблема**: Клик на "Создать событие" приводит к 404

---

## 🐛 ПРИЧИНА ПРОБЛЕМЫ

### ❌ **Несоответствие URL и структуры директорий**

**Что было сделано:**
```tsx
// user-menu-items.tsx:37
const handleCreateEventClick = () => {
  onItemClick?.();
  router.push("/events/new"); // ← Переход на /events/new
};
```

**Фактическая структура директорий:**
```
src/app/(app)/events/
├── [id]/           ← /events/:id
├── create/         ← /events/create ✅ (существует)
│   └── page.tsx
└── page.tsx        ← /events
```

**Проблема:**
- Код пытается открыть: `/events/new` ❌
- Страница существует по адресу: `/events/create` ✅
- Next.js не находит `/events/new` → **404 Not Found**

---

## 📂 СТРУКТУРА ФАЙЛОВОЙ СИСТЕМЫ

### Существующие маршруты:

1. ✅ `/events` → `src/app/(app)/events/page.tsx`
2. ✅ `/events/create` → `src/app/(app)/events/create/page.tsx`
3. ✅ `/events/[id]` → `src/app/(app)/events/[id]/page.tsx`
4. ✅ `/events/[id]/edit` → `src/app/(app)/events/[id]/edit/page.tsx`

### Несуществующие маршруты:

1. ❌ `/events/new` → **404 Not Found** (директории `new/` не существует)

---

## 🔍 ГЛУБОКИЙ АНАЛИЗ

### 1. Страница создания события существует

```tsx
// src/app/(app)/events/create/page.tsx
import { CreateEventPageContent } from "@/components/events/create-event-page-content";
import { getCurrentUser } from "@/lib/auth/currentUser";

export const dynamic = "force-dynamic";

export default async function CreateEventPage() {
  const currentUser = await getCurrentUser();

  return (
    <CreateEventPageContent 
      isAuthenticated={!!currentUser}
      userCityId={currentUser?.cityId ?? null}
    />
  );
}
```

**Вывод:** Страница полностью функциональна, просто находится по другому URL.

---

### 2. Компонент EventForm готов

Semantic search показал:
- ✅ `EventForm` компонент существует (`src/components/events/event-form.tsx`)
- ✅ Поддерживает `mode="create"` и `mode="edit"`
- ✅ Имеет все секции: BasicInfo, Locations, Vehicle, Rules, CustomFields
- ✅ Интегрирован с billing v2.0 (club plan limits)

---

### 3. Поиск других упоминаний `/events/new`

```bash
grep -r "events/new" src/
```

**Результат:** Не найдено других упоминаний.

**Вывод:** Только `user-menu-items.tsx` использует неверный URL.

---

## 🎯 ВАРИАНТЫ РЕШЕНИЯ

### ✅ **Вариант 1: Изменить URL в user-menu-items.tsx (Рекомендуется)**

**Преимущества:**
- ✅ Минимальное изменение (1 строка кода)
- ✅ Не трогаем существующие маршруты
- ✅ URL `/events/create` семантически правильный (REST convention)
- ✅ Работает немедленно

**Недостатки:**
- ❌ Нет (это правильное решение)

**Изменение:**
```tsx
// src/components/layout/user-menu-items.tsx:37
const handleCreateEventClick = () => {
  onItemClick?.();
  router.push("/events/create"); // ✅ Было: /events/new
};
```

---

### ❌ **Вариант 2: Переименовать директорию create/ в new/**

**Преимущества:**
- ✅ URL `/events/new` короче на 2 символа

**Недостатки:**
- ❌ Нужно переименовать директорию
- ❌ Нужно обновить все ссылки на `/events/create` в коде
- ❌ URL `/events/create` более стандартный (REST API convention)
- ❌ Может быть hardcoded где-то ещё (риск)
- ❌ Больше работы, больше риска

**НЕ рекомендуется.**

---

### ❌ **Вариант 3: Создать redirect /events/new → /events/create**

**Преимущества:**
- ✅ Оба URL будут работать

**Недостатки:**
- ❌ Лишний redirect (301/302)
- ❌ Дополнительный network round-trip
- ❌ Усложнение архитектуры
- ❌ Зачем поддерживать 2 URL для одной страницы?

**НЕ рекомендуется.**

---

## 🏗️ REST API CONVENTIONS

### Стандартные URL для CRUD операций:

```
GET    /events         → Список всех событий
GET    /events/:id     → Просмотр события
GET    /events/create  → Форма создания ✅ (принято в веб-приложениях)
POST   /events         → Создание события (API)
GET    /events/:id/edit → Форма редактирования
PATCH  /events/:id     → Обновление события (API)
DELETE /events/:id     → Удаление события (API)
```

**Rails/Laravel/Django/Spring:** Используют `/events/create` для форм.  
**Next.js App Router:** Тоже рекомендует `/events/create`.

### `/events/new` vs `/events/create`

**`/events/new`:**
- Короче на 2 символа
- Используется редко (в основном Ruby on Rails старых версий)

**`/events/create`:**
- ✅ Более явный и понятный
- ✅ Стандарт для большинства фреймворков
- ✅ Семантически правильный (глагол "создать")

---

## 🔍 ПРОВЕРКА ДРУГИХ КОМПОНЕНТОВ

### Есть ли другие места, где используется `/events/create`?

```bash
grep -r "events/create" src/
```

**Нужно проверить:**
1. ✅ `CreateEventPageContent` — может иметь внутренние ссылки
2. ✅ `EventsGrid` — кнопка "Создать событие"
3. ✅ Хлебные крошки (breadcrumbs)
4. ✅ Навигация после создания события

---

## 📊 ПРОВЕРКА EventsGrid

```tsx
// src/components/events/events-grid.tsx
import { CreateEventButton } from "@/components/events/create-event-button";
```

**Grep result:** `CreateEventButton` не найден.

**Возможные причины:**
1. Компонент удалён
2. Компонент переименован
3. Кнопка создания реализована по-другому

**Нужно проверить:** Есть ли кнопка "Создать событие" на странице `/events`?

---

## ✅ РЕКОМЕНДУЕМОЕ РЕШЕНИЕ

### **Изменить URL в user-menu-items.tsx**

**Единственное изменение:**
```diff
// src/components/layout/user-menu-items.tsx
const handleCreateEventClick = () => {
  onItemClick?.();
- router.push("/events/new");
+ router.push("/events/create");
};
```

**Почему это правильно:**
1. ✅ Минимальное изменение (1 строка)
2. ✅ Страница `/events/create` существует и работает
3. ✅ URL семантически правильный (REST convention)
4. ✅ Нет рисков для других частей приложения
5. ✅ Мгновенное решение проблемы

---

## 🧪 ТЕСТИРОВАНИЕ (После исправления)

### Desktop:
- [ ] Открыть dropdown меню (аватар)
- [ ] Клик "Создать событие"
- [ ] Ожидаем: переход на `/events/create` ✅
- [ ] Форма создания события загружается ✅

### Mobile:
- [ ] Открыть mobile drawer (hamburger)
- [ ] Клик "Создать событие"
- [ ] Ожидаем: переход на `/events/create` ✅
- [ ] Форма создания события загружается ✅

### Другие способы доступа:
- [ ] Прямой переход `/events/create` в браузере
- [ ] Есть ли кнопка "Создать событие" на `/events`?
- [ ] Навигация после создания события (redirect куда?)

---

## 📝 ИТОГ

**Проблема:** `/events/new` не существует, страница находится по адресу `/events/create`.

**Причина:** Ошибка в URL при добавлении пункта меню "Создать событие".

**Решение:** Изменить `router.push("/events/new")` → `router.push("/events/create")`.

**Сложность:** Минимальная (1 строка кода).

**Риски:** Отсутствуют.

---

## 🚀 ГОТОВ К ИСПРАВЛЕНИЮ

Жду подтверждения для применения исправления.
