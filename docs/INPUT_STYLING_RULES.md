# 📝 Правила стилизации Input элементов

## Дата: 15 декабря 2025

---

## ⚠️ ВАЖНО: Запрет локальных стилей

**Все стили должны быть встроены в компоненты!**

❌ **НЕЛЬЗЯ:**
```tsx
<input className="border-2 focus:ring-4 focus:ring-orange-500/20" />
<Input className="border-2 focus:ring-4" />
```

✅ **ПРАВИЛЬНО:**
```tsx
<Input />  // Все стили уже внутри компонента
<input type="search" className="..." />  // Только для нативных элементов с полным набором стилей
```

---

## 🎨 Стандартные стили Input компонента

### Базовые параметры:

```tsx
// src/components/ui/input.tsx
const Input = React.forwardRef<HTMLInputElement, InputProps>(
  ({ className, type, ...props }, ref) => {
    return (
      <input
        type={type}
        className={cn(
          // Base styles
          "flex h-12 w-full rounded-xl bg-white px-4 text-[15px] text-[#1F2937]",
          
          // Border - thin and subtle (1px)
          "border border-[#E5E7EB]",
          
          // Hover state - subtle lightening
          "hover:border-[#D1D5DB]",
          
          // Focus state - orange border only, NO ring
          "focus:border-[var(--color-primary)] focus:outline-none",
          
          // Transition for smooth color change
          "transition-colors",
          
          // Placeholder
          "placeholder:text-[#6B7280]",
          
          // Disabled state
          "disabled:cursor-not-allowed disabled:opacity-50 disabled:bg-[#F9FAFB]",
          
          // File input
          "file:border-0 file:bg-transparent file:text-sm file:font-medium",
          
          className
        )}
        ref={ref}
        {...props}
      />
    );
  }
);
```

---

## 📏 Спецификация стилей

### 1. Размеры

| Параметр | Значение | Tailwind |
|----------|----------|----------|
| **Высота** | 48px | `h-12` |
| **Padding X** | 16px | `px-4` |
| **Padding Y** | auto | (задается высотой) |
| **Радиус** | 12px | `rounded-xl` |
| **Шрифт** | 15px | `text-[15px]` |

### 2. Цвета

| Состояние | Цвет | Значение |
|-----------|------|----------|
| **Фон** | Белый | `bg-white` |
| **Текст** | Темно-серый | `text-[#1F2937]` |
| **Placeholder** | Серый | `text-[#6B7280]` |
| **Border (default)** | Светло-серый | `border-[#E5E7EB]` |
| **Border (hover)** | Серый | `border-[#D1D5DB]` |
| **Border (focus)** | Оранжевый | `var(--color-primary)` (#FF6F2C) |
| **Disabled bg** | Очень светло-серый | `bg-[#F9FAFB]` |

### 3. Толщина границ

| Состояние | Толщина | Tailwind |
|-----------|---------|----------|
| **Default** | 1px | `border` |
| **Hover** | 1px | `border` |
| **Focus** | 1px | `border` |

❌ **НЕ ИСПОЛЬЗУЕМ:** `border-2` (2px) - слишком толсто!

### 4. Focus состояние

| Эффект | Используется | Не используется |
|--------|--------------|-----------------|
| **Border color** | ✅ Orange | - |
| **Outline** | ❌ None | `focus:outline-none` |
| **Ring (glow)** | ❌ None | ~~`focus:ring-4`~~ |
| **Ring offset** | ❌ None | ~~`ring-offset-2`~~ |

**Почему нет ring?**
- Слишком навязчиво
- Отвлекает внимание
- Не нужно для хорошей UX
- Достаточно цветной границы

---

## 🎯 Визуальные примеры

### Default состояние:
```
┌────────────────────────┐
│  Введите текст...      │  ← 1px серая рамка
└────────────────────────┘
```

### Hover состояние:
```
┌────────────────────────┐
│  Введите текст...      │  ← 1px чуть темнее
└────────────────────────┘
```

### Focus состояние:
```
┌────────────────────────┐
│  Введите текст▎        │  ← 1px оранжевая рамка
└────────────────────────┘
(без свечения!)
```

### Disabled состояние:
```
╔════════════════════════╗
║  Недоступно            ║  ← 50% opacity, серый фон
╚════════════════════════╝
```

---

## 📋 Применение для разных элементов

### Input (из shadcn/ui):
```tsx
import { Input } from "@/components/ui/input";

// ✅ Правильно - используем компонент как есть
<Input 
  type="text"
  placeholder="Имя и фамилия"
  value={name}
  onChange={(e) => setName(e.target.value)}
/>

// ❌ НЕПРАВИЛЬНО - добавляем локальные стили
<Input 
  className="border-2 focus:ring-4"
  ...
/>
```

### Textarea (нативный элемент):
```tsx
// ✅ Правильно - применяем полный набор стандартных стилей
<textarea
  rows={3}
  className="w-full px-4 py-3 rounded-xl border border-[#E5E7EB] bg-white text-[15px] text-[#1F2937]
    hover:border-[#D1D5DB] focus:outline-none focus:border-[var(--color-primary)]
    transition-colors resize-none placeholder:text-[#6B7280]"
/>

// ❌ НЕПРАВИЛЬНО - неполные или отличающиеся стили
<textarea
  className="border-2 focus:ring-4 focus:ring-orange-500/20"
/>
```

### Search Input (нативный):
```tsx
// ✅ Правильно - как в clubs/page.tsx
<input
  type="search"
  placeholder="Поиск..."
  className="h-12 w-full rounded-xl border border-[#E5E7EB] bg-white pl-12 pr-4 text-[15px]
    placeholder:text-[#6B7280] transition-colors hover:border-[#D1D5DB]
    focus:border-[var(--color-primary)] focus:outline-none"
/>
```

---

## 🚫 Запрещенные практики

### 1. ❌ Локальная переопределение базовых стилей:
```tsx
// ПЛОХО - зачем переопределять то, что уже есть в компоненте?
<Input className="border-2" />
<Input className="focus:ring-4" />
<Input className="h-10" />  // меняет стандартную высоту
```

### 2. ❌ Разные стили в разных местах:
```tsx
// ПЛОХО - несогласованность
// В одном файле:
<input className="border-2 focus:ring-4" />

// В другом файле:
<input className="border focus:ring-2" />
```

### 3. ❌ Использование устаревших паттернов:
```tsx
// ПЛОХО - старый подход
<input className="ring-offset-background focus-visible:ring-2 focus-visible:ring-ring" />

// ХОРОШО - новый стандарт
<Input />  // всё уже внутри
```

---

## ✅ Правильные практики

### 1. Использование компонента без изменений:
```tsx
import { Input } from "@/components/ui/input";

<Input 
  placeholder="Email"
  value={email}
  onChange={(e) => setEmail(e.target.value)}
/>
// Все стили уже применены!
```

### 2. Добавление только специфичных утилит:
```tsx
// ✅ OK - добавляем только layout/spacing
<Input className="mb-4" />
<Input className="col-span-6" />

// ❌ НЕ OK - меняем базовые стили
<Input className="border-2 h-10" />
```

### 3. Создание специализированных компонентов:
```tsx
// ✅ Если нужен особый Input - создай новый компонент
export function SearchInput({ ...props }) {
  return (
    <div className="relative">
      <Search className="absolute left-4 top-1/2 -translate-y-1/2" />
      <Input className="pl-12" {...props} />
    </div>
  );
}
```

---

## 🔄 Миграция существующего кода

Если находишь код с локальными стилями:

### Шаг 1: Найти
```bash
# Поиск устаревших паттернов
grep -r "border-2.*focus:ring" src/
grep -r "ring-offset" src/
grep -r "focus-visible:ring" src/
```

### Шаг 2: Заменить
```tsx
// ❌ Старый код:
<input 
  className="border-2 border-gray-200 focus:ring-4 focus:ring-orange-500/20"
/>

// ✅ Новый код:
<Input />
```

### Шаг 3: Проверить
- Визуально проверить все состояния (default, hover, focus, disabled)
- Убедиться что анимации плавные
- Протестировать keyboard navigation

---

## 📚 Ссылки

- [Input Component](/src/components/ui/input.tsx)
- [Clubs Page Example](/src/app/clubs/page.tsx) - строка 145
- [Profile Page Example](/src/app/profile/page.tsx) - textarea
- [Design System Colors](/src/app/globals.css) - CSS переменные

---

## 🎨 Философия дизайна

### Принципы:

1. **Минимализм**
   - Меньше эффектов = меньше отвлечений
   - Focus ring не нужен если есть цветная граница

2. **Консистентность**
   - Одинаковые стили везде
   - Встроено в компоненты

3. **Производительность**
   - Меньше CSS = быстрее рендер
   - Нет box-shadow = меньше repaint

4. **Accessibility**
   - Focus состояние всегда видно
   - Достаточный контраст цветов
   - Keyboard navigation работает

---

## ✅ Чеклист для ревью кода

При добавлении новых форм проверь:

- [ ] Используется компонент `<Input />` а не нативный `<input>`
- [ ] Нет локальных стилей `border-2`, `focus:ring-4`
- [ ] Для `<textarea>` применен полный набор стандартных стилей
- [ ] Все input'ы выглядят одинаково
- [ ] Focus состояние без ring, только border
- [ ] Используются CSS переменные для цветов
- [ ] Плавная transition при изменении состояний

---

**Последнее обновление:** 15 декабря 2025  
**Статус:** ✅ Утверждено и обязательно к применению
