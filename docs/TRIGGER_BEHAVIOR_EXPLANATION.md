# Объяснение поведения триггера sync_event_club_flag

## 🎯 Вопрос

При тестировании миграций обнаружено:
```sql
INSERT INTO events (..., club_id, is_club_event) 
VALUES (..., NULL, TRUE);
-- Ожидалась ошибка, но запись успешно вставилась с is_club_event = FALSE
```

## ✅ Это правильное поведение!

### Порядок выполнения PostgreSQL

```
1. BEFORE INSERT триггер
   ↓
2. CHECK constraints
   ↓
3. INSERT в таблицу
```

### Что происходит конкретно

#### Шаг 1: Триггер (BEFORE INSERT)
```sql
CREATE TRIGGER trigger_sync_event_club_flag
  BEFORE INSERT OR UPDATE OF club_id ON public.events
  FOR EACH ROW
  EXECUTE FUNCTION public.sync_event_club_flag();
```

**Функция триггера:**
```sql
CREATE OR REPLACE FUNCTION public.sync_event_club_flag()
RETURNS TRIGGER AS $$
BEGIN
  -- Автоматически устанавливаем is_club_event на основе club_id
  NEW.is_club_event := (NEW.club_id IS NOT NULL);
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;
```

**В нашем случае:**
- Входные данные: `club_id = NULL`, `is_club_event = TRUE`
- Триггер видит: `NEW.club_id IS NOT NULL` → `FALSE`
- Триггер устанавливает: `NEW.is_club_event := FALSE`
- Триггер возвращает: `NEW` (с исправленным `is_club_event`)

#### Шаг 2: Проверка constraint
```sql
ALTER TABLE public.events
ADD CONSTRAINT events_club_consistency_check
  CHECK (
    (is_club_event = TRUE AND club_id IS NOT NULL) OR
    (is_club_event = FALSE AND club_id IS NULL)
  );
```

**В нашем случае:**
- Constraint видит: `is_club_event = FALSE` (уже исправлено триггером!)
- Constraint видит: `club_id = NULL`
- Проверка: `(FALSE = FALSE AND NULL IS NULL)` → **TRUE** ✅
- Constraint удовлетворен!

#### Шаг 3: INSERT
- Запись успешно вставляется в таблицу
- Финальное состояние: `club_id = NULL`, `is_club_event = FALSE`

---

## 🤔 Почему это лучше чем просто constraint?

### Вариант 1: Только constraint (без триггера)
```sql
-- Разработчик ошибся
INSERT INTO events (..., club_id, is_club_event) 
VALUES (..., NULL, TRUE);

-- ❌ ERROR: new row violates check constraint
-- Приложение падает, пользователь видит ошибку
```

**Минусы:**
- ❌ Хрупкий код (любая ошибка = crash)
- ❌ Приложение должно всегда помнить про синхронизацию
- ❌ Плохой UX для пользователя

### Вариант 2: Constraint + триггер (наше решение)
```sql
-- Разработчик ошибся
INSERT INTO events (..., club_id, is_club_event) 
VALUES (..., NULL, TRUE);

-- ✅ SUCCESS: is_club_event автоматически исправлен на FALSE
-- БД сама исправила ошибку!
```

**Плюсы:**
- ✅ **Самоисправление** на уровне БД
- ✅ Приложение работает даже при ошибках
- ✅ Constraint остается как fallback (если триггер отключен)
- ✅ **Принцип наименьшего удивления:** `club_id = NULL` → логично что `is_club_event = FALSE`

---

## 🔒 Когда constraint все-таки сработает?

Constraint сработает только если:
1. **Триггер отключен** (вручную через `DISABLE TRIGGER`)
2. **Прямое нарушение** (если обойти триггер через низкоуровневый доступ)

### Пример с отключенным триггером:
```sql
-- Отключить триггер
ALTER TABLE public.events DISABLE TRIGGER trigger_sync_event_club_flag;

-- Теперь попытаться вставить
INSERT INTO events (..., club_id, is_club_event) 
VALUES (..., NULL, TRUE);

-- ❌ ERROR: new row violates check constraint "events_club_consistency_check"
-- Constraint сработал, так как триггер не исправил данные!

-- Включить триггер обратно
ALTER TABLE public.events ENABLE TRIGGER trigger_sync_event_club_flag;
```

---

## 📊 Таблица поведения

| Входные данные | Триггер включен | Результат | Constraint |
|----------------|-----------------|-----------|------------|
| `club_id=NULL`, `is_club_event=TRUE` | ✅ Да | **Успех:** `is_club_event` → `FALSE` | Удовлетворен |
| `club_id=NULL`, `is_club_event=TRUE` | ❌ Нет | **Ошибка** constraint | Нарушен |
| `club_id='uuid'`, `is_club_event=FALSE` | ✅ Да | **Успех:** `is_club_event` → `TRUE` | Удовлетворен |
| `club_id='uuid'`, `is_club_event=FALSE` | ❌ Нет | **Ошибка** constraint | Нарушен |
| `club_id=NULL`, `is_club_event=FALSE` | ✅/❌ | **Успех** (уже корректно) | Удовлетворен |
| `club_id='uuid'`, `is_club_event=TRUE` | ✅/❌ | **Успех** (уже корректно) | Удовлетворен |

---

## 🎓 Архитектурное решение

Это пример **defensive programming** на уровне базы данных:

### Слои защиты (от внешнего к внутреннему):

1. **Application Layer** (TypeScript/Service Layer)
   - Должен правильно устанавливать `is_club_event`
   - Но может ошибиться

2. **Trigger Layer** (BEFORE INSERT/UPDATE)
   - **Автоматически исправляет** ошибки приложения
   - Гарантирует консистентность даже при багах в коде
   - **Первая линия защиты**

3. **Constraint Layer** (CHECK)
   - **Блокирует** прямое нарушение целостности
   - Работает если триггер отключен
   - **Последняя линия защиты**

### Аналогия:
```
Application → "Может ошибиться"
Trigger     → "Я исправлю!"
Constraint  → "Если триггер спит, я не пропущу!"
```

---

## ✅ Вывод

**Поведение триггера корректно и желательно.**

- ✅ Триггер **умнее** чем просто constraint
- ✅ Он **исправляет** ошибки вместо того, чтобы падать
- ✅ Constraint остается как **страховка**
- ✅ Это **best practice** для критичных полей

**Не нужно "чинить" триггер** - он работает именно так, как задумано!

---

## 📝 Обновленный тест

См. обновленный **Тест 7** в `00_APPLY_MIGRATIONS.md`, который теперь проверяет:
- ✅ Автоматическое исправление `club_id=NULL, is_club_event=TRUE` → `is_club_event=FALSE`
- ✅ Автоматическое исправление `club_id='uuid', is_club_event=FALSE` → `is_club_event=TRUE`

Оба случая должны **успешно выполниться** с автоисправлением значений.

---

_Этот документ объясняет архитектурное решение и почему "баг" на самом деле является фичей._

