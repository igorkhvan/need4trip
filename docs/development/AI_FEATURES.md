# AI Features - Need4Trip

## Обзор

Need4Trip использует OpenAI для AI-powered функций, которые улучшают пользовательский опыт при создании событий.

---

## 🤖 Реализованные Функции

### 1. AI-генерация Правил Событий

**Endpoint:** `POST /api/ai/events/generate-rules`

**Описание:**  
Автоматически генерирует структурированные правила участия на основе данных события (категория, тип авто, кастомные поля, и т.д.).

**Использование:**
- Кнопка "Сгенерировать правила (ИИ)" в форме создания/редактирования события
- Заполняет поле "Правила участия" AI-сгенерированным текстом
- Пользователь может отредактировать текст перед сохранением

**Требования:**
- Пользователь должен быть авторизован
- Обязательные поля: title, categoryId, cityId, locationText

**Что генерируется:**
- Общая информация о событии
- Поведение и дисциплина в колонне
- Требования к автомобилю (адаптируется под категорию)
- Безопасность
- Что взять с собой (инструменты, запчасти)
- Дополнительные условия (из кастомных полей)
- Ответственность участника / дисклеймер

---

## 🔧 Настройка

### Environment Variables

Добавьте в `.env.local`:

```env
# OpenAI API Key (required for AI features)
OPENAI_API_KEY=sk-proj-...

# OpenAI Model (optional, default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
```

### Получение OpenAI API Key

1. Зарегистрируйтесь на https://platform.openai.com/
2. Создайте новый API ключ: https://platform.openai.com/api-keys
3. Добавьте ключ в `.env.local`
4. **Важно:** Не коммитьте `.env.local` в git!

### Стоимость

**Рекомендуемая модель:** `gpt-4o-mini`  
**Стоимость:** ~$0.15 / 1M input tokens, ~$0.60 / 1M output tokens  
**Средний запрос:** ~400-600 output tokens = ~$0.0004 за генерацию

**Оценка:** ~2,500 генераций за $1

---

## 🏗️ Архитектура

### Backend Components

```
/src/lib/services/ai/
  ├── openai.ts              # OpenAI client wrapper
  └── eventDataResolver.ts   # ID → name resolution

/src/lib/types/
  └── ai.ts                  # Zod schemas & types

/src/app/api/ai/events/generate-rules/
  └── route.ts               # API endpoint
```

### Data Flow

```
EventForm (UI)
    ↓ (click button)
API Call to /api/ai/events/generate-rules
    ↓
Validate with Zod
    ↓
Resolve IDs → Names (using cached repos)
    ↓
Build AI Prompts (system + user)
    ↓
Call OpenAI API
    ↓
Return rulesText
    ↓
Fill textarea in EventForm
```

### ID Resolution

Функция `resolveEventData()` использует **кэшированные repositories**:
- `eventCategoryRepo` - категории событий (TTL: 1 час)
- `cityRepo` - города (TTL: 1 час)
- `carBrandRepo` - марки авто (TTL: 24 часа)

**Преимущества:**
- ✅ Быстро (данные в памяти)
- ✅ Не нагружает БД
- ✅ Консистентно с остальным приложением

---

## 🔒 Безопасность

### Authentication
- Требуется авторизованный пользователь (`getCurrentUser()`)
- Неавторизованные запросы возвращают 401

### API Key Security
- `OPENAI_API_KEY` хранится **только на сервере**
- Никогда не отправляется клиенту
- Используется только в API routes (server-side)

### Rate Limiting
**Статус:** TODO (не реализовано)

**Рекомендации для production:**
- Implement per-user rate limit (e.g., 10 requests per hour)
- Use Redis or in-memory cache
- Return 429 status when limit exceeded

---

## 📊 Мониторинг

### Логирование

Все AI запросы логируются:

```typescript
log.info("AI rules generation requested", {
  userId: currentUser.id,
  eventId: eventData.eventId,
  title: eventData.title,
});

log.info("AI rules generated successfully", {
  userId: currentUser.id,
  rulesLength: rulesText.length,
  tokens: usage.total_tokens,
});
```

### Метрики для отслеживания

- Количество AI генераций в день
- Средняя длина сгенерированного текста
- Количество ошибок
- Стоимость токенов
- Процент редактирования (сколько пользователей меняют AI текст)

---

## 🧪 Тестирование

### Ручное Тестирование

1. **Создание события:**
   - Заполните обязательные поля (название, категория, город)
   - Нажмите "Сгенерировать правила (ИИ)"
   - Проверьте что правила загрузились в textarea
   - Отредактируйте (опционально)
   - Сохраните событие

2. **Редактирование события:**
   - Откройте существующее событие на редактирование
   - Измените какие-то данные (категория, тип авто)
   - Нажмите "Сгенерировать правила (ИИ)" снова
   - Проверьте что правила обновились

3. **Error Cases:**
   - Попробуйте без авторизации → должен вернуть 401
   - Попробуйте с пустыми обязательными полями → должен показать ошибку
   - Неправильный API key → должен показать user-friendly ошибку

### Unit Tests (TODO)

```typescript
// tests/ai/eventDataResolver.test.ts
describe('resolveEventData', () => {
  it('resolves category name from ID', async () => { /* ... */ });
  it('resolves city name from ID', async () => { /* ... */ });
  it('resolves brand names from IDs', async () => { /* ... */ });
  it('handles missing optional data', async () => { /* ... */ });
});

// tests/ai/openai.test.ts (with mocked OpenAI)
describe('generateText', () => {
  it('calls OpenAI with correct params', async () => { /* ... */ });
  it('handles API errors gracefully', async () => { /* ... */ });
  it('returns generated text', async () => { /* ... */ });
});
```

---

## 🚀 Deployment

### Vercel Environment Variables

Добавьте в Vercel dashboard:

1. Settings → Environment Variables
2. Add variable:
   - **Name:** `OPENAI_API_KEY`
   - **Value:** `sk-proj-...`
   - **Environments:** Production, Preview, Development

### Проверка после деплоя

```bash
# Test AI generation endpoint
curl -X POST https://your-domain.com/api/ai/events/generate-rules \
  -H "Content-Type: application/json" \
  -H "Cookie: your-auth-cookie" \
  -d '{"title":"Test","categoryId":"...","cityId":"...","locationText":"..."}'
```

---

## 📝 Prompt Engineering

### System Prompt

Определяет роль AI и формат ответа:
- Опытный организатор внедорожных поездок
- Структурированный вывод (обязательные разделы)
- Русский язык
- Bullet points
- Конкретика, никакой воды

**Расположение:** `src/lib/services/ai/openai.ts::buildRulesSystemPrompt()`

### User Prompt

Содержит данные конкретного события:
- Название, описание
- Дата, время, место
- Категория, тип авто
- Кастомные поля
- Платная/бесплатная

**Расположение:** `src/lib/services/ai/eventDataResolver.ts::buildUserPrompt()`

### Улучшение Промптов

Если качество генерации не устраивает:

1. **Измените system prompt** - добавьте больше инструкций
2. **Расширьте user prompt** - добавьте больше контекста
3. **Измените temperature** - меньше (0.5) = более предсказуемо, больше (0.9) = более креативно
4. **Увеличьте maxTokens** - если текст обрезается

---

## 🔮 Future Enhancements

### Возможные улучшения:

1. **Вариативность:**
   - Кнопка "Регенерировать" для других вариантов
   - Выбор тона: строгий / дружелюбный / юмористический

2. **AI Ассистент:**
   - Умные подсказки при заполнении формы
   - Автокоррекция ошибок
   - Предложение категории на основе названия

3. **Мультиязычность:**
   - Генерация на английском, казахском
   - Автоперевод правил

4. **Analytics:**
   - Tracking usage
   - A/B testing разных промптов
   - Feedback loop (пользователь оценивает качество)

---

## ❓ FAQ

**Q: Что делать если OpenAI API не работает?**  
A: Функция gracefully degraded - пользователь может написать правила вручную.

**Q: Можно ли использовать другую AI модель?**  
A: Да, установите `OPENAI_MODEL=gpt-4o` в `.env.local`.

**Q: Сколько стоит каждая генерация?**  
A: С моделью `gpt-4o-mini` примерно $0.0004 за запрос.

**Q: Нужен ли rate limiting?**  
A: Для production - да. Добавьте Redis или in-memory rate limiter.

**Q: Можно ли кастомизировать промпт для разных категорий?**  
A: Да, модифицируйте `buildRulesSystemPrompt()` чтобы принимать categoryCode и изменять инструкции.
