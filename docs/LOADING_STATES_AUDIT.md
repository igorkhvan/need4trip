# 🔄 Аудит состояний загрузки (Loading States)

## 📊 Текущее состояние

### Существующие компоненты

#### 1. **Spinner Component** (`src/components/ui/spinner.tsx`)
```typescript
- Spinner (sm/md/lg) - оранжевый крутящийся круг
- PageLoader - спиннер + текст в центре
- FullPageLoader - брендированный полноэкранный loader
```

#### 2. **Next.js Loading Files**
- `src/app/loading.tsx` - FullPageLoader (root level)
- `src/app/events/loading.tsx` - Skeleton + Spinner
- `src/app/events/create/loading.tsx` - (exists)
- `src/app/events/[id]/loading.tsx` - (exists)

---

## 🔍 Аудит по страницам

### ✅ Хорошие примеры

#### 1. **Events Loading** (`/events/loading.tsx`)
**Статус:** ✅ Отлично
- **Используется:** Skeleton cards (6 штук) + Spinner внизу
- **Дизайн:** Соответствует структуре контента
- **UX:** Показывает предполагаемую структуру

#### 2. **Event Edit** (`/events/[id]/edit/page.tsx`)
**Статус:** ✅ Хорошо
- **Используется:** Skeleton для всех элементов формы
- **Дизайн:** Соответствует реальной форме

---

### ⚠️ Проблемные места

#### 1. **Clubs Page** (`/clubs/page.tsx`)
**Статус:** ⚠️ Проблема
- **Client Component** - состояние `loading`
- **Используется:** Кастомный inline spinner (не компонент!)
```tsx
<div className="inline-block h-8 w-8 animate-spin rounded-full border-b-2 border-[var(--color-primary)]"></div>
```
- **Проблема:** 
  - Нет skeleton, сразу пустой экран
  - Кастомный спиннер вместо компонента
  - Нет Next.js loading.tsx файла

#### 2. **Events Page** (`/events/page.tsx`)
**Статус:** ⚠️ Несоответствие
- **Client Component** - состояние `loading`
- **Используется:** Серый skeleton (устаревший стиль)
```tsx
<div className="h-20 bg-gray-200 rounded-xl" />
```
- **Проблема:**
  - Используется `bg-gray-200` вместо `bg-[#F7F7F8]`
  - Простые блоки, не skeleton карточек
  - Есть loading.tsx, но он не используется (SSR страница)

#### 3. **Profile Page** (`/profile/page.tsx`)
**Статус:** ⚠️ Кастомный спиннер
- **Client Component** - состояние `loading`
- **Используется:** Inline спиннер (не компонент)
```tsx
<div className="h-8 w-8 animate-spin rounded-full border-4 border-[var(--color-primary)] border-t-transparent"></div>
```
- **Проблема:**
  - Нет skeleton для структуры профиля
  - Кастомный spinner вместо компонента

#### 4. **Profile Edit** (`/profile/edit/page.tsx`)
**Статус:** ❌ Импортирует Loader2 (не существует)
```tsx
import { Loader2 } from "lucide-react";
<Loader2 className="h-8 w-8 animate-spin text-gray-400" />
```
- **Проблема:**
  - Импорт из lucide-react
  - Не использует стандартный Spinner

#### 5. **Pricing Page** (`/pricing/page.tsx`)
**Статус:** ❌ Только текст
```tsx
<p className="text-muted-foreground">Загрузка тарифов...</p>
```
- **Проблема:** Нет визуального индикатора

#### 6. **EventForm Component** (`/components/events/event-form.tsx`)
**Статус:** ⚠️ Inline loader для категорий
```tsx
{loadingCategories ? (
  <div className="h-12 w-full rounded-xl border border-[#E5E7EB] bg-gray-50 flex items-center justify-center text-[#6B7280]">
    Загрузка категорий...
  </div>
) : ...}
```
- **Проблема:** Кастомный inline loader

#### 7. **Events Grid** (`/components/events/events-grid.tsx`)
**Статус:** ⚠️ State для loadingCategories, но нет UI индикатора

---

### ✅ Server Components (хорошо)

#### 1. **Club Details** (`/clubs/[id]/page.tsx`)
**Статус:** ✅ Server Component
- **Нет loading state** - Next.js обрабатывает автоматически
- **Использует:** Suspense boundaries от Next.js

#### 2. **Event Details** (`/events/[id]/page.tsx`)
**Статус:** ✅ Server Component
- **Нет loading state** - Next.js обрабатывает автоматически

#### 3. **Homepage** (`/page.tsx`)
**Статус:** ✅ Server Component
- **Нет loading state** - Next.js обрабатывает автоматически

---

## 📋 Сводная таблица проблем

| Страница/Компонент | Тип | Текущее состояние | Проблема |
|-------------------|-----|-------------------|----------|
| `/clubs` | Client | Inline spinner | ❌ Нет skeleton, кастомный код |
| `/events` | Client | Gray skeleton | ⚠️ Устаревший стиль |
| `/profile` | Client | Inline spinner | ❌ Нет skeleton, кастомный код |
| `/profile/edit` | Client | Loader2 (lucide) | ❌ Неправильный импорт |
| `/pricing` | Client | Только текст | ❌ Нет визуала |
| `EventForm` | Component | Inline loader | ⚠️ Кастомный код |
| `EventsGrid` | Component | Нет UI | ⚠️ loadingCategories без UI |
| `/events/loading.tsx` | SSR | Skeleton + Spinner | ✅ Хорошо |
| `/events/[id]/edit` | Client | Skeleton | ✅ Хорошо |

---

## 🎯 Выводы

### Проблемы:

1. **Несоответствие подходов:**
   - Server Components используют Next.js loading.tsx
   - Client Components используют inline spinners
   - Нет единого стиля

2. **Кастомные инлайн-спиннеры:**
   - `/clubs` - свой spinner
   - `/profile` - свой spinner
   - `/profile/edit` - Loader2 из lucide

3. **Отсутствие скелетонов:**
   - `/clubs` - сразу пустой экран
   - `/profile` - только спиннер
   - `/pricing` - только текст

4. **Устаревшие стили:**
   - `bg-gray-200` вместо `bg-[#F7F7F8]`
   - Некоторые используют правильные цвета

5. **Несогласованность:**
   - Где-то skeleton
   - Где-то spinner
   - Где-то ничего

---

## 💡 Варианты решений

### Вариант 1: "Минимальная унификация"

**Подход:** Заменить все кастомные спиннеры на компонент `Spinner`

**Изменения:**
- ✅ Заменить inline spinners на `<Spinner />` или `<PageLoader />`
- ✅ Исправить цвета в существующих skeleton (`bg-gray-200` → `bg-[#F7F7F8]`)
- ❌ НЕ добавлять skeleton где их нет

**Плюсы:**
- Быстро (2-3 часа)
- Минимальные изменения
- Единый spinner компонент

**Минусы:**
- Нет skeleton для структурированных данных
- UX не улучшается, только унифицируется

**Затронутые файлы:** ~6 файлов

---

### Вариант 2: "Skeleton First" (Рекомендуемый)

**Подход:** Создать skeleton для каждого типа контента

**Изменения:**
1. Создать skeleton компоненты:
   - `<ClubCardSkeleton />` - для карточек клубов
   - `<EventCardSkeleton />` - для карточек событий
   - `<ProfileSkeleton />` - для страницы профиля
   - `<FormSkeleton />` - для форм редактирования

2. Добавить `loading.tsx` файлы:
   - `/clubs/loading.tsx` - skeleton grid 3x4
   - `/profile/loading.tsx` - skeleton профиля

3. Заменить все inline loaders на:
   - Skeleton - для структурированных данных (списки, карточки, формы)
   - Spinner - только для простых операций (кнопки, малые компоненты)

**Плюсы:**
- Лучший UX - пользователь видит структуру контента
- Единый стиль
- Соответствует best practices

**Минусы:**
- Больше работы (1-2 дня)
- Нужно создать несколько компонентов

**Затронутые файлы:** ~15 файлов

---

### Вариант 3: "Full Loading System" (Максимальный)

**Подход:** Полная система загрузки с transitions

**Изменения:**
1. Все из Варианта 2
2. Добавить:
   - `<SuspenseWrapper />` - обертка с fallback
   - `useLoadingTransition()` - хук для плавных переходов
   - Streaming SSR где возможно
   - Оптимистичный UI для форм

3. Продвинутые паттерны:
   - Skeleton появляется после 300ms (избегаем flash)
   - Progressive loading для списков
   - Placeholder с реальными данными из кэша

**Плюсы:**
- Превосходный UX
- Production-ready
- Плавные переходы

**Минусы:**
- Много работы (3-4 дня)
- Сложнее в поддержке

**Затронутые файлы:** ~25+ файлов

---

## 🎨 Design System для Skeleton

### Цвета (уже в проекте)
```tsx
bg-[#F7F7F8]  // Основной для skeleton
bg-[#E5E7EB]  // Border для карточек
```

### Анимация
```tsx
animate-pulse  // Tailwind встроенная
```

### Размеры (соответствуют контенту)
```tsx
h-7   // Заголовок
h-4   // Текст
h-12  // Кнопка/Input
h-32  // Карточка
```

---

## 📌 Рекомендации

**Я рекомендую Вариант 2: "Skeleton First"**

**Причины:**
1. ✅ Оптимальный баланс усилий/результата
2. ✅ Значительное улучшение UX
3. ✅ Соответствует современным практикам
4. ✅ Не перегружает проект сложностью
5. ✅ Легко расширяется в будущем

**Порядок внедрения (если выберете Вариант 2):**

### Этап 1: Создание базовых компонентов (30 мин)
1. `<ClubCardSkeleton />`
2. `<EventCardSkeleton />`
3. `<ProfileHeaderSkeleton />`
4. `<FormFieldSkeleton />`

### Этап 2: Страницы списков (45 мин)
1. `/clubs/loading.tsx` - skeleton grid
2. Обновить `/clubs/page.tsx` - использовать Spinner во время пагинации

### Этап 3: Страницы профиля (45 мин)
1. `/profile/loading.tsx` - skeleton профиля
2. Исправить `/profile/page.tsx` - использовать компонент
3. Исправить `/profile/edit/page.tsx` - skeleton формы

### Этап 4: Остальные страницы (30 мин)
1. `/pricing/page.tsx` - добавить spinner
2. Обновить `/events/page.tsx` - правильные цвета
3. `EventForm` - skeleton для категорий

### Этап 5: Документация (15 мин)
1. Создать `LOADING_STATES_GUIDE.md`
2. Примеры использования

**Итого:** ~2.5 часа чистого времени

---

## ❓ Какой вариант выбираем?

Жду твоего решения! 🎯
