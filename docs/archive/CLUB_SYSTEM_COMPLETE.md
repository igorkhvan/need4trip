# Club System - Complete Implementation Guide

## 📋 ОБЗОР

Полная реализация системы клубов, подписок и прав доступа для Need4Trip.

**Статус:** ✅ **ЗАВЕРШЕНО** (Phases 0-7)  
**Дата:** 12 декабря 2024  
**Версия:** 1.0.0

---

## 🗂️ СТРУКТУРА ПРОЕКТА

### Database Schema (6 миграций)

```
supabase/migrations/
├── 20241212_create_clubs.sql                      # Таблица clubs
├── 20241212_create_club_members.sql               # Таблица club_members + constraints
├── 20241212_create_club_subscriptions.sql         # Таблица club_subscriptions + triggers
├── 20241212_alter_events_club_and_visibility.sql  # Расширение events (club_id, visibility)
├── 20241212_alter_users_add_plan.sql              # Расширение users (plan)
└── 20241212_create_initial_triggers.sql           # Триггеры синхронизации
```

**Новые таблицы:**
- `clubs` - Информация о клубах
- `club_members` - Участники клубов с ролями
- `club_subscriptions` - Подписки клубов

**Расширенные таблицы:**
- `events` - добавлено `club_id`, обновлен `visibility`
- `users` - добавлено `plan` (free/pro)

---

## 🔧 BACKEND INFRASTRUCTURE

### Type System

```
src/lib/types/
├── club.ts        # ClubRole, ClubPlan, Club, ClubMember, ClubSubscription
├── user.ts        # UserPlan + расширен User
└── event.ts       # Расширен Visibility (public/unlisted/restricted)
```

**Новые типы:**
- `ClubRole`: owner | organizer | member | pending
- `ClubPlan`: club_free | club_basic | club_pro
- `UserPlan`: free | pro
- `Visibility`: public | unlisted | restricted (расширен)

### Repository Layer

```
src/lib/db/
├── clubRepo.ts           # CRUD для clubs (9 функций)
├── clubMemberRepo.ts     # CRUD для club_members (13 функций)
└── subscriptionRepo.ts   # CRUD для подписок (12 функций)
```

**Функции:**
- `clubRepo`: create, get, update, delete, list, search, listByCreator
- `clubMemberRepo`: add, get, update, remove, list, approve, count
- `subscriptionRepo`: get, create, update, upgrade, downgrade, list expiring

### Service Layer

```
src/lib/services/
├── permissions.ts    # Permissions Engine (686 строк, 14 функций)
├── clubs.ts          # Бизнес-логика клубов (581 строка)
└── subscriptions.ts  # Бизнес-логика подписок (284 строки)
```

**Permissions Engine (ключевые функции):**
- `canCreateEvent()` - проверка лимитов создания событий
- `canEditEvent()` - проверка прав на редактирование
- `canViewEvent()` - проверка видимости события
- `canUseVisibility()` - проверка доступа к видимости
- `canCreatePaidEvent()` - проверка платных событий
- `canManageClub()` - проверка прав управления клубом
- `canManageClubMembers()` - проверка прав управления участниками
- `validateEventCreation()` - валидация создания события

**Правила бизнес-логики:**
```
Free User:     1 активное личное событие
Pro User:      ∞ личных событий + платные

Club Free:     1 активное событие
Club Basic:    3 активных события
Club Pro:      ∞ событий

Visibility:
- public:      Публичный (доступно всем)
- unlisted:    Скрытый (по ссылке доступно всем)
- restricted:  Скрытый (по ссылке доступно авторизованным)
```

### API Routes

```
src/app/api/
├── clubs/
│   ├── route.ts                          # GET, POST /clubs
│   ├── [id]/route.ts                     # GET, PATCH, DELETE /clubs/[id]
│   ├── [id]/members/route.ts             # GET, POST /clubs/[id]/members
│   ├── [id]/members/[userId]/route.ts    # PATCH, DELETE
│   └── [id]/subscription/route.ts        # GET, PATCH /clubs/[id]/subscription
└── profile/
    ├── route.ts                          # GET /profile
    └── plan/route.ts                     # GET, PATCH /profile/plan
```

**Endpoints (7 новых):**
- 3 для clubs
- 3 для club members
- 1 для club subscription
- 2 для profile

---

## 🎨 FRONTEND COMPONENTS

### Club Components

```
src/components/clubs/
├── club-card.tsx                 # Карточка клуба
├── club-form.tsx                 # Форма создания/редактирования
├── club-members-list.tsx         # Список участников с управлением
└── club-subscription-card.tsx    # Карточка подписки
```

### Profile Components

```
src/components/profile/
├── user-plan-card.tsx       # Карточка личного плана
├── user-clubs-list.tsx      # Список клубов пользователя
└── user-stats-card.tsx      # Статистика пользователя
```

### UI Components (расширенные)

```
src/components/ui/
├── badge.tsx           # +premium, +default варианты
└── confirm-dialog.tsx  # +controlled mode, +loading state
```

---

## 📄 PAGES

### Club Pages

```
src/app/clubs/
├── page.tsx                 # Список клубов с поиском
├── create/page.tsx          # Создание клуба
├── [id]/page.tsx            # Детали клуба
└── [id]/manage/page.tsx     # Управление клубом (owner/organizer)
```

### Profile Page

```
src/app/profile/
└── page.tsx                 # Профиль пользователя
```

**Особенности страниц:**
- SSR (Server-Side Rendering)
- `dynamic = "force-dynamic"` для свежих данных
- Проверка прав доступа на серверной стороне
- Redirects для неавторизованных
- `notFound()` для несуществующих ресурсов

---

## 🔐 СИСТЕМА ПРАВ ДОСТУПА

### Роли в клубе

| Роль        | Права                                                      |
|-------------|------------------------------------------------------------|
| owner       | Полные права (управление, участники, подписка, удаление)   |
| organizer   | Создание событий, редактирование клуба                      |
| member      | Участие в событиях клуба                                    |
| pending     | Ожидает подтверждения                                       |

### Лимиты подписок

**Личные планы (UserPlan):**
| План  | События | Платные | Видимость          | Клубы |
|-------|---------|---------|--------------------| ------|
| free  | 1       | ✗       | public только      | 1     |
| pro   | ∞       | ✓       | все                | ∞     |

**Клубные планы (ClubPlan):**
| План        | События | Платные | Видимость | Участники |
|-------------|---------|---------|-----------|-----------|
| club_free   | 1       | ✗       | public    | ∞         |
| club_basic  | 3       | ✓       | все       | ∞         |
| club_pro    | ∞       | ✓       | все       | ∞         |

---

## 📊 СТАТИСТИКА РЕАЛИЗАЦИИ

### Код

```
Backend:
  Migrations:   6 файлов, ~300 строк SQL
  Types:        3 файла, ~400 строк TS
  Repositories: 3 файла, ~850 строк TS
  Services:     3 файла, ~1550 строк TS
  API Routes:   7 файлов, ~460 строк TS
  
Frontend:
  Components:   7 файлов, ~1070 строк TSX
  Pages:        5 файлов, ~620 строк TSX
  UI Updates:   2 файла, ~50 строк TSX
  
ИТОГО: ~5300 строк типизированного кода
```

### Функциональность

- ✅ **6 SQL миграций** с triggers и constraints
- ✅ **3 новых репозитория** (34 функции)
- ✅ **3 новых сервиса** (permissions engine, clubs, subscriptions)
- ✅ **7 API endpoints** для clubs и profile
- ✅ **7 React компонентов** для clubs и profile
- ✅ **5 страниц** (clubs + profile)
- ✅ **Полная система прав доступа**
- ✅ **Лимиты и валидация**

---

## 🧪 ТЕСТИРОВАНИЕ

### Ручные тесты

**Database:**
```sql
-- Проверка миграций
SELECT * FROM clubs;
SELECT * FROM club_members;
SELECT * FROM club_subscriptions;
SELECT * FROM events WHERE club_id IS NOT NULL;
SELECT * FROM users WHERE plan = 'pro';
```

**API:**
```bash
# Список клубов
curl http://localhost:3000/api/clubs

# Создание клуба
curl -X POST http://localhost:3000/api/clubs \
  -H "Content-Type: application/json" \
  -d '{"name":"Test Club","city":"Moscow"}'

# Профиль
curl http://localhost:3000/api/profile
```

**Pages:**
- [ ] `/clubs` - отображение списка
- [ ] `/clubs?q=moscow` - поиск работает
- [ ] `/clubs/create` - форма создания
- [ ] `/clubs/[id]` - детали клуба
- [ ] `/clubs/[id]/manage` - редактирование (только owner)
- [ ] `/profile` - профиль пользователя

---

## 🚀 DEPLOYMENT CHECKLIST

### Before Production

- [ ] Применить все миграции к production БД
- [ ] Регенерировать Supabase types: `npx supabase gen types typescript`
- [ ] Убрать все `(client as any)` casts из репозиториев
- [ ] Убрать все `TODO: Need4Trip` комментарии
- [ ] Настроить переменные окружения
- [ ] Интегрировать платежную систему (Stripe/Yookassa)
- [ ] Добавить email-уведомления
- [ ] Настроить мониторинг и логирование

### Environment Variables

```env
# Обязательные
NEXT_PUBLIC_APP_URL=https://your-domain.com
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...
AUTH_JWT_SECRET=...

# Опциональные (для платежей)
STRIPE_SECRET_KEY=...
STRIPE_WEBHOOK_SECRET=...
```

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ ДОКУМЕНТЫ

- [SYSTEM_OVERVIEW.md](./SYSTEM_OVERVIEW.md) - Обзор существующей системы
- [PHASE_0_CODEBASE_ANALYSIS.md](./PHASE_0_CODEBASE_ANALYSIS.md) - Анализ кодовой базы
- [PHASE_1_MIGRATIONS.md](./PHASE_1_MIGRATIONS.md) - Детали миграций
- [TRIGGER_BEHAVIOR_EXPLANATION.md](./TRIGGER_BEHAVIOR_EXPLANATION.md) - Поведение триггеров

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ (Future Roadmap)

### Краткосрочные (1-2 недели)
- [ ] Интеграция платежной системы
- [ ] Email-уведомления для участников клуба
- [ ] Приглашения в клубы (invite links)
- [ ] Запросы на вступление в клуб

### Среднесрочные (1-2 месяца)
- [ ] Модерация событий клуба
- [ ] Статистика клубов (аналитика)
- [ ] Кастомные роли в клубах
- [ ] Клубные чаты (интеграция Telegram)

### Долгосрочные (3+ месяца)
- [ ] Федерация клубов
- [ ] Межклубные события
- [ ] Рейтинговая система клубов
- [ ] Мобильное приложение

---

## 🤝 ПОДДЕРЖКА

По вопросам реализации обращаться к документации или коммитам:
- `050d137` - PHASE 4: Service Layer
- `cf5eb16` - PHASE 5: API Routes
- `3777a47` - PHASE 6: Frontend Components
- `8463a59` - PHASE 7: Pages

**Автор:** AI Assistant  
**Дата создания:** 12 декабря 2024  
**Последнее обновление:** 12 декабря 2024

