# 📋 ПЛАН ДЕЙСТВИЙ: Навигация в пользовательском меню

**Дата**: 2025-12-22  
**Задача**: Добавить "Создать событие" и "Мои события" в desktop/mobile меню

---

## 🎯 ЦЕЛЬ

Улучшить UX навигации для авторизованных пользователей:
1. Быстрый доступ к созданию события (2 клика вместо 3+)
2. Быстрый доступ к своим событиям (напрямую из меню)
3. Улучшенная мобильная шторка (справа, шире, удобнее)

---

## 📐 АРХИТЕКТУРА

### Принципы
- ✅ **DRY**: Один компонент `UserMenuItems` для всех вариантов
- ✅ **Separation of Concerns**: Навигация ≠ логика данных
- ✅ **Progressive Enhancement**: URL параметры для sharing
- ✅ **Design System**: Используем существующие компоненты и стили

### Затрагиваемые файлы
1. `src/components/layout/user-menu-items.tsx` — добавить пункты
2. `src/components/layout/mobile-nav.tsx` — изменить направление/ширину
3. `src/components/events/events-grid.tsx` — поддержка URL параметров
4. `src/app/(app)/events/page.tsx` — передать searchParams в EventsGrid

---

## 🔧 ЭТАП 1: UserMenuItems — Добавить пункты навигации

### Цель
Добавить "Создать событие" и "Мои события" в меню авторизованных пользователей.

### Изменения

#### 1.1 Импорты
```tsx
import { User, LogOut, PlusCircle, Calendar } from "lucide-react";
```

#### 1.2 Handler для навигации
```tsx
const handleCreateEventClick = () => {
  onItemClick?.();
  router.push("/events/new");
};

const handleMyEventsClick = () => {
  onItemClick?.();
  router.push("/events?tab=my");
};
```

#### 1.3 Порядок пунктов

**Variant: dropdown**
```
[User Info]
───────────
Создать событие
Мои события
Профиль
───────────
Выйти
```

**Variant: sheet**
```
[User Info Card]

Создать событие
Мои события
Профиль

Выйти
```

#### 1.4 Стили
- **Иконки**: `h-4 w-4` (dropdown), `h-5 w-5` (sheet)
- **Цвета**: primary для "Создать", default для "Мои события"
- **Hover**: стандартные bg transitions

### Валидация
- ✅ Только для авторизованных (`currentUser !== null`)
- ✅ Закрытие меню перед навигацией (`onItemClick`)
- ✅ Консистентность стилей с существующими пунктами

---

## 🔧 ЭТАП 2: MobileNav — Изменить направление и ширину

### Цель
Открывать drawer справа + увеличить ширину для touch-friendly UI.

### Изменения

#### 2.1 Направление
```tsx
// Было:
<SwipeableSheetContent side="left" ...>

// Стало:
<SwipeableSheetContent side="right" ...>
```

**Эффект**:
- Drawer открывается справа
- Свайп вправо закрывает (автоматически)
- Анимация: `slide-in-from-right`

#### 2.2 Ширина
```tsx
// Было:
className="w-[280px] sm:w-[320px]"

// Стало:
className="w-[320px] sm:w-[400px]"
```

**Обоснование**:
- **320px mobile**: 80-85% viewport на iPhone SE / малых экранах
- **400px tablet+**: комфортная ширина для touch (44px+ кнопки)
- **Best practices**: Apple HIG, Material Design Nav Drawer

### Валидация
- ✅ Кнопка Menu остаётся слева (hamburger icon)
- ✅ Свайп работает в правильном направлении
- ✅ Оверлей закрывает drawer при клике
- ✅ Адаптивная ширина на разных экранах

---

## 🔧 ЭТАП 3: EventsGrid — Поддержка URL параметров

### Цель
Поддержать `?tab=my` в URL для:
1. Sharing ссылок (прямой переход на "Мои события")
2. Bookmarking
3. Browser back/forward навигация

### Изменения

#### 3.1 Читать tab из URL
```tsx
// В page.tsx передаём searchParams
<EventsGrid 
  events={events}
  currentUserId={currentUserId}
  isAuthenticated={isAuthenticated}
  initialTab={searchParams?.tab} // ← новый prop
/>
```

#### 3.2 Инициализация activeTab
```tsx
// В EventsGrid
const [activeTab, setActiveTab] = useState<TabType>(
  (initialTab as TabType) || "all"
);
```

#### 3.3 Обновление URL при клике
```tsx
onChange={(tabId) => {
  startTransition(() => {
    setActiveTab(tabId as TabType);
    setCurrentPage(1);
    
    // Обновляем URL
    const params = new URLSearchParams(window.location.search);
    if (tabId === "all") {
      params.delete("tab");
    } else {
      params.set("tab", tabId);
    }
    const newUrl = params.toString() 
      ? `/events?${params.toString()}`
      : "/events";
    router.push(newUrl, { scroll: false });
  });
}}
```

### Валидация
- ✅ URL `/events?tab=my` напрямую активирует таб
- ✅ URL `/events` без параметра → таб "all"
- ✅ Невалидный `?tab=invalid` → fallback на "all"
- ✅ Browser back/forward работает корректно
- ✅ Scroll position не сбрасывается (`scroll: false`)

---

## 🔧 ЭТАП 4: page.tsx — Передать searchParams

### Цель
Пробросить URL параметры из Server Component в Client Component.

### Изменения

#### 4.1 Типы
```tsx
interface EventsPageProps {
  searchParams?: { 
    city?: string;
    tab?: string; // ← добавляем
  };
}
```

#### 4.2 Передача props
```tsx
return (
  <EventsGrid 
    events={events}
    currentUserId={currentUserId}
    isAuthenticated={isAuthenticated}
    initialTab={searchParams?.tab} // ← передаём
  />
);
```

### Валидация
- ✅ Server Component → Client Component data flow
- ✅ Type safety с TypeScript
- ✅ SSR-friendly (работает без JS)

---

## 🧪 ЭТАП 5: Тестирование

### Desktop Dropdown Menu
- [ ] Открыть dropdown → видны все пункты
- [ ] Клик "Создать событие" → переход на `/events/new`
- [ ] Клик "Мои события" → переход на `/events?tab=my`
- [ ] Таб "Мои события" активен автоматически
- [ ] Клик "Профиль" → работает как раньше
- [ ] Клик "Выйти" → работает как раньше
- [ ] Dropdown закрывается после каждого клика

### Mobile Sheet Drawer
- [ ] Кнопка Menu (hamburger) открывает drawer **справа**
- [ ] Drawer ширина: 320px (mobile), 400px (tablet+)
- [ ] Свайп **вправо** закрывает drawer
- [ ] Клик на overlay закрывает drawer
- [ ] Все пункты навигации видны и кликабельны
- [ ] "Создать событие" → переход + закрытие drawer
- [ ] "Мои события" → переход + закрытие drawer
- [ ] Touch-friendly размеры (min 44px height кнопок)

### URL параметры
- [ ] Прямой переход `/events?tab=my` → таб "Мои события" активен
- [ ] Клик на таб → URL обновляется
- [ ] Browser back → возврат к предыдущему табу
- [ ] Browser forward → вперёд по истории
- [ ] Копирование URL → работает при вставке в новой вкладке
- [ ] Без параметра `?tab` → таб "Все события" активен

### Edge Cases
- [ ] Неавторизованный пользователь:
  - Desktop dropdown: только "Войти"
  - Mobile drawer: только "Войти через Telegram"
- [ ] Пользователь без событий:
  - "Мои события" показывается
  - Переход → EmptyState в EventsGrid
- [ ] Быстрые клики (double-click):
  - Навигация не ломается
  - Drawer закрывается корректно

### Responsive
- [ ] Mobile (< 640px): drawer 320px
- [ ] Tablet (640px - 1024px): drawer 400px
- [ ] Desktop (> 1024px): dropdown menu корректно
- [ ] iPhone SE (375px): drawer не перекрывает весь экран
- [ ] iPad (768px): drawer не слишком узкий

### Accessibility
- [ ] Tab navigation работает
- [ ] Focus visible states корректны
- [ ] Screen reader labels корректны
- [ ] Escape закрывает drawer/dropdown

---

## 📦 COMMIT STRATEGY

### Commit 1: UserMenuItems — Navigation items
```
feat(ui): добавить навигацию в пользовательское меню

Проблема:
- Пользователям нужно 3+ клика чтобы создать событие
- Нет быстрого доступа к "Мои события"

Решение:
- Добавлены пункты "Создать событие" и "Мои события"
- Порядок: Create → My Events → Profile | Logout
- Иконки: PlusCircle, Calendar
- Работает для desktop dropdown и mobile sheet

Изменения:
✅ src/components/layout/user-menu-items.tsx
```

### Commit 2: MobileNav — Direction and width
```
refactor(ui): изменить направление и ширину mobile drawer

Проблема:
- Mobile drawer слишком узкий (280px)
- Открывается слева (нестандартно для iOS/Android)

Решение:
- Изменено направление: right (стандарт индустрии)
- Увеличена ширина: 320px/400px (touch-friendly)
- Свайп вправо для закрытия (интуитивно)

Best practices:
- Apple HIG: 320-375px для sidebar
- Material Design: 280-360px для nav drawer
- Min 44px height для touch targets

Изменения:
✅ src/components/layout/mobile-nav.tsx
```

### Commit 3: EventsGrid — URL tab support
```
feat(events): поддержка URL параметра ?tab=my

Проблема:
- Нельзя делиться ссылкой на "Мои события"
- Browser back/forward не работают для табов
- Нет deep linking

Решение:
- Чтение ?tab из URL при загрузке
- Обновление URL при клике на таб
- Поддержка browser navigation
- SSR-friendly (работает без JS)

URL примеры:
- /events → таб "Все события"
- /events?tab=my → таб "Мои события"
- /events?tab=upcoming → таб "Предстоящие"

Изменения:
✅ src/app/(app)/events/page.tsx
✅ src/components/events/events-grid.tsx
```

---

## ⚠️ РИСКИ И МИТИГАЦИЯ

### Риск 1: Ломается существующая навигация
**Вероятность**: Низкая  
**Митигация**: Минимальные изменения, не трогаем существующий роутинг

### Риск 2: Конфликт со свайпом на touch устройствах
**Вероятность**: Низкая  
**Митигация**: Используем проверенный `SwipeableSheetContent`

### Риск 3: URL параметры ломают существующие фильтры
**Вероятность**: Средняя  
**Митигация**: Сохраняем все существующие параметры (`city`, `search`)

### Риск 4: Drawer слишком широкий на малых экранах
**Вероятность**: Низкая  
**Митигация**: 320px = 85% от 375px (iPhone SE) — комфортно

---

## ✅ КРИТЕРИИ УСПЕХА

1. ✅ Desktop dropdown содержит 4 пункта (Create, My, Profile, Logout)
2. ✅ Mobile drawer открывается справа с шириной 320px/400px
3. ✅ Свайп вправо закрывает drawer
4. ✅ URL `/events?tab=my` активирует таб "Мои события"
5. ✅ Build проходит без ошибок
6. ✅ TypeScript не ругается
7. ✅ Линтер не выдаёт ошибок
8. ✅ Все тесты проходят (если есть)

---

## 🚀 ГОТОВ К РЕАЛИЗАЦИИ

**Время выполнения**: ~30 минут  
**Сложность**: Low (минимальные изменения, нет новых компонентов)  
**Затрагиваемые файлы**: 4 файла (user-menu-items, mobile-nav, events-grid, events/page)

**Следующий шаг**: Получить подтверждение → начать реализацию поэтапно.
