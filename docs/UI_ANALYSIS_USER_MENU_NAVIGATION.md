# 🔍 АНАЛИЗ: Навигация в пользовательском меню

**Дата**: 2025-12-22  
**Задача**: Добавить "Создать событие" и "Мои события" в меню для авторизованных пользователей

---

## 📋 ТРЕБОВАНИЯ ПОЛЬЗОВАТЕЛЯ

### Desktop (Dropdown Menu)
1. ✅ Добавить пункт "Создать событие"
2. ✅ Добавить пункт "Мои события" → открывать `/events?tab=my`
3. ✅ Сохранить существующие пункты: "Профиль", "Выйти"

### Mobile (Sheet Drawer)
1. ✅ Добавить те же пункты в мобильную шторку
2. ✅ Изменить направление открытия: **справа** (было слева)
3. ✅ Изменить жест закрытия: **свайп вправо** (было влево)
4. ✅ Увеличить ширину шторки согласно best practices

---

## 🔍 ТЕКУЩАЯ АРХИТЕКТУРА

### 1. **HeaderUserSection** (`header-user-section.tsx`)
- Desktop dropdown меню
- Рендерит `UserMenuItems` с `variant="dropdown"`
- Показывает аватар для авторизованных, иконку User для гостей
- Использует `DropdownMenu` из shadcn/ui

### 2. **MobileNav** (`mobile-nav.tsx`)
- Mobile drawer (sheet) с hamburger кнопкой
- Рендерит:
  - `navItems` (События, Клубы, Тарифы)
  - `UserMenuItems` с `variant="sheet"`
- Использует `SwipeableSheetContent` с `side="left"` и `width: 280px/320px`
- Свайп жест: влево для закрытия

### 3. **UserMenuItems** (`user-menu-items.tsx`)
- Переиспользуемый компонент для обоих вариантов
- **Текущие пункты**:
  - User Info (имя + email/handle)
  - **Профиль** (User icon)
  - **Выйти** (LogOut icon)
- Поддерживает `variant="dropdown"` и `variant="sheet"`

### 4. **EventsGrid** (`events-grid.tsx`)
- Страница `/events` с табами
- **Табы**: "Все события", "Предстоящие", "Мои события"
- Таб "Мои события" фильтрует по `createdByUserId === currentUserId`
- URL параметры: можно передать `?tab=my` (но сейчас не используется)

---

## 🎯 ЧТО НУЖНО ИЗМЕНИТЬ

### 1. ✅ UserMenuItems — добавить пункты навигации

**Добавляемые пункты (только для авторизованных)**:
1. **Создать событие** → `/events/new` (иконка: `PlusCircle`)
2. **Мои события** → `/events?tab=my` (иконка: `Calendar`)

**Порядок пунктов**:
```
[User Info Card]
───────────────
Создать событие
Мои события
Профиль
───────────────
Выйти
```

### 2. ✅ MobileNav — изменить направление и ширину

**Изменения**:
- `side="left"` → `side="right"`
- `width: 280px/320px` → `width: 320px/400px` (best practices для touch UI)
- Свайп направление изменится автоматически (right → swipe right to close)

### 3. ✅ EventsGrid — поддержать URL параметр `?tab=my`

**Изменения**:
- Читать `searchParams.tab` из URL
- Устанавливать `activeTab` из URL при загрузке
- При клике на таб — обновлять URL (для sharing/bookmarking)

---

## 📐 BEST PRACTICES: Ширина Mobile Sheet

### Текущая ширина
```tsx
w-[280px] sm:w-[320px]  // 280px mobile, 320px tablet
```

### Рекомендуемая ширина (Apple HIG, Material Design)
- **Mobile (< 640px)**: `320px - 360px` (80-90% viewport)
- **Tablet (640px+)**: `375px - 400px` (fixed width)

### Обоснование
1. **Touch-friendly**: больше места для кнопок (min 44px height)
2. **Визуально комфортно**: не слишком узко, не перекрывает весь экран
3. **Стандарт индустрии**: iOS Sidebar (320-375px), Android Nav Drawer (280-360px)

### Итоговая ширина
```tsx
w-[320px] sm:w-[400px]  // 320px mobile, 400px tablet+
```

---

## 🛠️ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### SwipeableSheetContent
- **Текущий код**: `mobile-nav.tsx:107`
- **Компонент**: Обёртка над `SheetContent` с поддержкой свайпов
- **Свайп логика**: определяется автоматически по `side` prop:
  - `side="left"` → swipe left to close
  - `side="right"` → swipe right to close

### Sheet Component
- **Файл**: `components/ui/sheet.tsx`
- **Базовый компонент**: Radix UI Dialog
- **Анимации**: `data-[state=open]:slide-in-from-{direction}`
- **Ширина**: задаётся через `className` на `SheetContent`

### События навигации
- `/events/new` — создание нового события (существующий маршрут)
- `/events?tab=my` — страница событий с активным табом "Мои события"

---

## 🔒 АРХИТЕКТУРНЫЕ ПРОВЕРКИ

### ✅ 1. Функциональная целостность
- Навигация не ломает существующую логику
- Роутинг остаётся прежним
- Авторизация не меняется

### ✅ 2. Переиспользование компонентов
- `UserMenuItems` остаётся универсальным
- Один компонент для desktop + mobile
- Единая логика навигации

### ✅ 3. UX консистентность
- Пункты меню в одном порядке везде
- Иконки соответствуют действию
- Touch-friendly размеры

### ✅ 4. Прогрессивное улучшение
- Работает с и без JavaScript (базовые ссылки)
- URL параметры позволяют делиться ссылками
- Graceful degradation

---

## 🧪 EDGE CASES

### 1. Неавторизованный пользователь
- ❌ Не показываем "Создать событие" и "Мои события"
- ✅ Показываем только "Войти через Telegram"

### 2. Пользователь без событий
- ✅ "Мои события" показываем всегда (если авторизован)
- ✅ При переходе — показывается пустое состояние (EmptyState в EventsGrid)

### 3. Навигация из шторки
- ✅ Закрываем drawer перед переходом (`onItemClick`)
- ✅ Предотвращаем "двойные клики" через состояние загрузки

### 4. URL sharing
- ✅ `/events?tab=my` работает для всех (но показывает только свои события)
- ✅ Если пользователь неавторизован — показывается модалка авторизации

---

## 📊 СХЕМА НАВИГАЦИИ (Desktop Dropdown)

```
┌─────────────────────────────────────────┐
│  [IG]  Igor                             │
│        ikhvan@live.com                  │
├─────────────────────────────────────────┤
│  ⊕  Создать событие                     │
│  📅  Мои события                         │
│  👤  Профиль                             │
├─────────────────────────────────────────┤
│  ⎋  Выйти                               │ (красный)
└─────────────────────────────────────────┘
```

## 📊 СХЕМА НАВИГАЦИИ (Mobile Sheet)

```
┌──────────────────────────────────────────┐
│  Навигация                          [X]  │
├──────────────────────────────────────────┤
│                                          │
│  События                                 │
│  Клубы                                   │
│  Тарифы                                  │
│                                          │
├──────────────────────────────────────────┤
│                                          │
│  ┌────────────────────────────────────┐ │
│  │  Igor                              │ │
│  │  @igorkhvan                        │ │
│  └────────────────────────────────────┘ │
│                                          │
│  ⊕  Создать событие                     │
│  📅  Мои события                         │
│  👤  Профиль                             │
│                                          │
│  ⎋  Выйти                               │
│                                          │
└──────────────────────────────────────────┘
       ← swipe right to close
```

**Ширина**: 320px (mobile) / 400px (tablet+)  
**Направление**: справа

---

## ✅ ЧЕКЛИСТ ИЗМЕНЕНИЙ

### Phase 1: UserMenuItems
- [ ] Добавить импорт иконок: `PlusCircle`, `Calendar`
- [ ] Добавить пункт "Создать событие" → `/events/new`
- [ ] Добавить пункт "Мои события" → `/events?tab=my`
- [ ] Сохранить порядок: Create, My, Profile | Logout
- [ ] Применить для обоих вариантов (dropdown + sheet)

### Phase 2: MobileNav
- [ ] Изменить `side="left"` → `side="right"`
- [ ] Изменить ширину: `w-[320px] sm:w-[400px]`
- [ ] Проверить свайп направление (автоматически)
- [ ] Проверить анимации (slide-in-from-right)

### Phase 3: EventsGrid
- [ ] Читать `searchParams.tab` при загрузке
- [ ] Устанавливать `activeTab` из URL
- [ ] При клике на таб — обновлять URL (`router.push`)
- [ ] Сохранять другие параметры (city, search) в URL

### Phase 4: Testing
- [ ] Desktop dropdown — все пункты видны
- [ ] Mobile drawer — открывается справа
- [ ] Свайп вправо — закрывает drawer
- [ ] Клик на "Мои события" → таб активируется
- [ ] URL `/events?tab=my` — работает напрямую
- [ ] Неавторизованные — не видят новые пункты

---

## 🚀 ГОТОВ К РЕАЛИЗАЦИИ

Все изменения минимальные, архитектурно корректные, не ломают существующий код.

**Следующий шаг**: Получить подтверждение на план → начать реализацию.
